# Paraíso Náutico PE

Landing page responsiva em Next.js 15, TypeScript, Tailwind CSS, React Hook Form e Lucide React.

## Rodar localmente

```bash
npm install
npm run dev
```

Depois abra `http://localhost:3000`.
